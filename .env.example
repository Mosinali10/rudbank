# Server Configuration
PORT=5000

# Database Configuration
# For Local PostgreSQL:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/rudbank

# For Neon Cloud (Production):
DATABASE_URL=postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/database?sslmode=require

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=1d

# CORS Configuration
# For Development (allow all):
# CORS_ORIGIN=*

# For Production (specific domain):
CORS_ORIGIN=https://your-app.vercel.app

# Google OAuth (Optional)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Notes:
# 1. Never commit .env file to git (already in .gitignore)
# 2. For Vercel: Add these in Project Settings → Environment Variables
# 3. For Neon: Get DATABASE_URL from Neon Dashboard → Connection Details
# 4. JWT_SECRET should be a long random string in production
# 5. CORS_ORIGIN must match your frontend domain exactly in production
